name: CodeQL

# Temporarily disabled until end of April 2026 to conserve GitHub Actions minutes.
# Re-enable by restoring push/pull_request/schedule triggers.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: [self-hosted, d-sorg-fleet-4core]
    steps:
      - run: echo "CodeQL temporarily disabled — re-enable after 2026-05-01"
